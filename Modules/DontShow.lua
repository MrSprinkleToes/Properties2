local module = {}

-- archivable & className are commented out because i added checks for deprecated properties
-- just left them here because i was lazy
-- RobloxLocked used to commented out for the same reason but then for some reason
-- it stopped having the Deprecated tag

module.DontShow = {
	"RobloxLocked", -- roblox please add tags to this in the api dump
	---"archivable",
	--"className"
}

return module
